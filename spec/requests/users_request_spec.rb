require 'rails_helper'

RSpec.describe 'users', type: :request do
  describe '- testing for posts controller' do
    # it 'checks for correct response status, templtae, body to match with place holder text' do
    #   get '/users'
    #   expect(response.status).to eq(200)
    #   expect(response).to render_template('users/index')
    #   expect(response.body).to include('This page will display list of users along with their photos')
    # end

    # it 'checks for correct response status, templtae, body to match with place holder text' do
    #   get '/users/1'
    #   expect(response.status).to eq(200)
    #   expect(response).to render_template('users/show')
    #   expect(response.body).to include('This page will display user posts and etc')
    # end
  end
end
